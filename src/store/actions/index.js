import { getUsers, searchUsers, clearUsers } from './users';
import { getRepos, clearRepos, getSingleRepo } from './repos';

export { getUsers, searchUsers, clearUsers, getRepos, clearRepos, getSingleRepo };
